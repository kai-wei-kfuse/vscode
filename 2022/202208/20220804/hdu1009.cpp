#include <iostream>
using namespace std;

pair<int, int> a[5], b[5];

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        int x, y;
        for (int i = 1; i <= 4; i++) {
            cin >> x >> y;
            a[i].first = x;
            a[i].second = y;
        }
        for (int i = 1; i <= 4; i++) {
            cin >> x >> y;
            b[i].first = x;
            b[i].second = y;
        }
        int lmd, miu;
        a[2].first
    }
}