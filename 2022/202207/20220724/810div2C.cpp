#include <iostream>
using namespace std;

int a[100005];

int main() {
    int t;
    int n, m, k;
    while (t--) {
        cin >> n >> m >> k;
        for (int i = 1; i <= k; i++) {
            cin >> a[i];
        }
        
    }
}