#include<bits/stdc++.h>
using namespace std;
int n, q;
vector<int> tr(300005);
void add(int i,int x){
    while(i<=n){
        
    }
}
int main(){
    cin >> n >> q;
    vector<int> ve(n + 1);
    for (int i = 1; i <= n;i++){
        cin >> ve[i];
    }
}