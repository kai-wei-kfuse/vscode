#include<bits/stdc++.h>
using namespace std;
int main(){
    int t;
    cin >> t;
    while(t--){
        int n, k;
        cin >> n >> k;
        vector<int, int> s(n + 1);
        for (int i = 1; i <= n;i++){
            cin >> s[i];
        }
        for (int i = 1; i <= k;i++){

        }
    }
}